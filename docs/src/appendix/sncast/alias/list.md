# `list`

List aliases from the current effective `snfoundry.toml` configuration (resolved from `snfoundry.toml` config files according to the [precedence rules](../../../projects/configuration.md#interaction-between-local-and-global-profiles)).
