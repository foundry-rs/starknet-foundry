[package]
name = "test_script"
version = "0.1.0"

# See more keys and their definitions at https://docs.swmansion.com/scarb/docs/reference/manifest.html

[dependencies]
starknet = ">=2.3.0"
sncast_std = { git = "https://github.com/foundry-rs/starknet-foundry.git", tag = "v0.13.1" }
map = { path = "../../contracts/map" }
constructor_with_params = { path = "../../contracts/constructor_with_params" }

[lib]
sierra = true
casm = true

[[target.starknet-contract]]
sierra = true
casm = true

build-external-contracts = [
    "map::Mapn",
    "constructor_with_params::ConstructorWithParamsn"
]
