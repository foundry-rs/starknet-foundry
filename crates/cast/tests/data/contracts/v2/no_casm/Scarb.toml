[package]
name = "build_fails"
version = "0.1.0"

[dependencies]
starknet = ">=2.0.2"

[[target.starknet-contract]]
sierra = true
casm = false

[lib]
sierra = false

[tool.sncast]
url = "http://127.0.0.1:5055/rpc"
network = "testnet"
account = "user2"
