[package]
name = "fibonacci_virtual"
version = "0.1.0"

[dependencies]
addition = { path = "../addition_virtual" }
starknet.workspace = true

[[target.starknet-contract]]
sierra = true
casm = true
build-external-contracts = ["addition::AdditionContract"]

[lib]
