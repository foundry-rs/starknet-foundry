[package]
name = "targets"
version = "0.1.0"

[dependencies]
starknet = "2.7.0"

[dev-dependencies]
snforge_std = { path = "../../../../../../snforge_std" }

[[target.starknet-contract]]

[[test]]
name = "custom_first"
kind = "test"
source-path = "./tests/tests.cairo"
test-type = "integration"
build-external-contracts = ["targets::*"]

[tool.snforge]
exit_first = false
