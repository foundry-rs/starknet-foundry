[package]
name = "with_features"
version = "0.1.0"
edition = "2023_01"

[dependencies]
starknet = "2.7.0"

[dev-dependencies]
snforge_std = { path = "../../../../../../snforge_std" }

[[target.starknet-contract]]

[tool.snforge]
exit_first = false

[features]
enable_for_tests = []
