[package]
name = "erc20_package"
version = "0.1.0"

[dependencies]
starknet = "2.8.2"

[dev-dependencies]
snforge_std = { path = "../../../../../snforge_std" }

[[target.starknet-contract]]
sierra = true

[tool.snforge]
exit_first = false
