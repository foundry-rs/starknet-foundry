[package]
name = "features"
version = "0.1.0"
edition = "2024_07"

[dependencies]
starknet = "2.4.0"

[dev-dependencies]
snforge_std = { path = "../../../../../snforge_std" }

[[target.starknet-contract]]

[features]
snforge_test_only = []
